module.exports = function (grunt) {
    return {
        project: {
            options: {
                hostname: 'localhost',
                port: '4949',
                base: 'html5up-hyperspace',
                keepalive: true
            }
        }
    };
};